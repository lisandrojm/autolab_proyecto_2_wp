import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "path";
export default defineConfig({
  root: path.resolve(__dirname, "frontend"), // código fuente en frontend
  plugins: [react()],
  optimizeDeps: {
    exclude: ["lucide-react"],
  },
  build: {
    outDir: path.resolve(__dirname, "dist"), // <-- build final en la raíz
    emptyOutDir: true,
  },
});
